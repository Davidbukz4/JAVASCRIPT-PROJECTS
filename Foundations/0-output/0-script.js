/* DOMContentLoaded loads the js file even if it is called in the head or at
    the top of the body section of the HTML file */
document.addEventListener('DOMContentLoaded', function(){
    /* innerHTML defines the HTML content, it's the common way to display data */
    document.getElementById("demo").innerHTML = 5 + 6;
})
